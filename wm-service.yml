apiVersion: v1
kind: Service
metadata:
  name: world-monitor-app-service
  namespace: world-monitor-namespace
spec:
  type: ClusterIP
  selector:
    app: world-monitor-app-deployment
  ports:
  - port: 8001
    targetPort: 8001
