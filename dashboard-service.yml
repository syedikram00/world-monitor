apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: world-monitor-namespace
spec:
  type: ClusterIP
  selector:
    app: dashboard-deployment
  ports:
  - port: 8000
    targetPort: 8000
