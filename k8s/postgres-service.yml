apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: world-monitor-namespace
spec:
  type: ClusterIP
  selector:
    app: postgres-deployment
  ports:
  - port: 5432
    targetPort: 5432
